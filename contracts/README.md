## Smart contracts with Waffle, Mocha and Chai for testing

### Install dependencies:
	npm install

### Build smart contract ABI and bytecode files:
	npm run build

### Test (uses ts-node to transpile TS to JS)
    npm t